# مستندات بازطراحی رابط کاربری پلتفرم پارس لیان

این مستند شامل جزئیات تغییرات اعمال شده در بازطراحی کامل رابط کاربری (UI) و تجربه کاربری (UX) پلتفرم پارس لیان است. تمامی تغییرات با حفظ منطق بک‌اند و با هدف مدرن‌سازی، بهبود پاسخگویی (Responsiveness) و رفع خطاهای ساختاری انجام شده است.

## ۱. اصول طراحی جدید
طراحی جدید بر پایه استانداردهای مدرن وب و با تمرکز بر سادگی و کارایی انجام شده است:
- **رویکرد اول-موبایل (Mobile-First):** تمامی صفحات در تمامی دستگاه‌ها (موبایل، تبلت و دسکتاپ) به صورت کاملاً ریسپانسیو نمایش داده می‌شوند.
- **سیستم گرید پویا:** استفاده از CSS Grid برای چیدمان‌های منعطف (۱ ستونه در موبایل، ۲ ستونه در تبلت و ۳ یا ۴ ستونه در دسکتاپ).
- **هویت بصری یکپارچه:** استفاده از پالت رنگی استاندارد (Slate برای متون، Blue برای برندینگ، Emerald برای موفقیت و Amber برای هشدارها).

## ۲. کامپوننت‌های استاندارد شده
برای حفظ یکپارچگی در تمام صفحات، کامپوننت‌های جدیدی طراحی و جایگزین شدند:
- **`<x-enhanced-card>`:** کارت‌های مدرن با قابلیت سایه (Shadow)، انیمیشن‌های ورود و لایه‌های شیشه‌ای (Backdrop Blur).
- **`<x-enhanced-table>`:** جداول ریسپانسیو با پشتیبانی از اسکرول افقی در موبایل و استایل‌های Hover مدرن.
- **`<x-enhanced-status-badge>`:** نشانگرهای وضعیت با رنگ‌بندی داینامیک و انیمیشن‌های Pulse برای وضعیت‌های حساس.
- **`<x-page-header>`:** هدر یکپارچه برای تمامی صفحات با قابلیت پشتیبانی از عنوان، زیرعنوان، نشان (Badge) و دکمه‌های عملیاتی سریع.

## ۳. ویژگی‌های تعاملی و فنی
- **انیمیشن‌های بصری:** استفاده از `animate-blob` برای پس‌زمینه‌های متحرک، `animate-fade-in` و `animate-slide-up` برای ورود نرم المان‌ها.
- **محاسبات آنی (Real-time):** پیاده‌سازی جاوااسکریپت برای محاسبه هزینه‌ها، فرمت‌دهی پول (ریال) و جستجوی سریع در لیست‌ها بدون بارگذاری مجدد صفحه.
- **بهبود فرم‌ها:** طراحی مجدد فیلدها با استایل `form-control-modern` و افزودن پیش‌نمایش فایل‌های پیوستی.
- **رفع خطاهای ساختاری:** تمامی ۲۳ خطای گزارش شده در لینتر (شامل مشکلات CSS، تداخل‌های Blade و سینتکس جاوااسکریپت) به طور کامل رفع شد.

## ۴. تغییرات به تفکیک ماژول‌ها

### الف) مدیریت سفارشات خدمات (Service Orders)
- بازطراحی کامل فرم‌های ایجاد و ویرایش.
- افزودن بخش آمار سریع (Quick Stats) در بالای لیست سفارشات.
- بهبود نمایش وضعیت‌ها و تاریخ‌های شمسی.

### ب) انبار و قطعات (Inventory)
- سیستم مدیریت موجودی با نمایش بصری وضعیت انبار.
- جداول قطعات با قابلیت فیلتر سریع.
- طراحی مجدد فرم‌های ورود و خروج کالا.

### ج) تعمیرات و هزینه‌ها (Repairs)
- پنل تخصصی برای تکنسین‌ها با دسترسی سریع به لیست قطعات مصرفی.
- محاسبه خودکار مجموع هزینه‌های قطعات و اجرت در لحظه.
- نمایش تایم‌لاین فعالیت‌های انجام شده روی هر دستگاه.

### د) پورتال مشتریان (Customer Portal)
- داشبورد اختصاصی برای مشتریان با نمایش وضعیت گارانتی و سفارشات فعال.
- سیستم پیگیری سفارش با کد رهگیری و نمایش مراحل پیشرفت به صورت بصری.
- پروفایل کاربری مدرن با قابلیت ویرایش اطلاعات تماس.

### ه) حسابداری و فروش (Accounting)
- فرم‌های ایجاد فاکتور با طراحی متمرکز و فیلدهای عددی فرمت‌دهی شده.
- مدیریت مالی شفاف‌تر با نمایش سود و زیان و گزارشات آماری.

## ۵. ملاحظات امنیتی و عملکردی
- **بهبود سرعت بارگذاری:** حذف کدهای تکراری و بهینه‌سازی استایل‌های درون‌خطی (Inline Styles).
- **جلوگیری از تداخل:** کدهای جدید با حفظ کامل متغیرها و مسیرهای (Routes) قبلی نوشته شده‌اند تا خللی در عملکرد سیستم ایجاد نشود.
- **تأییدیه عملیات:** افزودن دیالوگ‌های تأیید (Confirmation) برای حذف یا تغییرات حساس با استفاده از SweetAlert2.

---
*این مستند به عنوان راهنمای نهایی تغییرات فاز اول بازطراحی پلتفرم پارس لیان تدوین شده است.*
