# راهنمای امنیت سیستم پارس لیان

## ⚠️ هشدار امنیتی بحرانی

مشکلات امنیتی شناسایی شده در فایل `config/database.php` برطرف شده‌اند. لطفاً این راهنما را دقیقاً دنبال کنید.

## مشکلات امنیتی رفع شده

### 1. ✅ اعتبارنامه‌های ناامن پایگاه داده
- **قبل**: استفاده از کاربر `root` بدون رمز عبور
- **بعد**: کاربر اختصاصی `pars_lian_user` با رمز عبور امن

### 2. ✅ حالت دیباگ فعال
- **قبل**: `APP_DEBUG=true` در محیط production
- **بعد**: `APP_DEBUG=false` و `APP_ENV=production`

### 3. ✅ مقادیر پیش‌فرض ناامن
- **قبل**: استفاده از مقادیر پیش‌فرض Laravel ('forge', 'forge', '')
- **بعد**: مقادیر امن و اختصاصی

## مراحل تنظیم امنیت

### مرحله ۱: اجرای اسکریپت پایگاه داده

```bash
# اتصال به MySQL به عنوان root
mysql -u root -p

# اجرای اسکریپت ایجاد کاربر امن
source database_user_setup.sql
```

### مرحله ۲: به‌روزرسانی فایل .env

فایل `.env.secure` را که ایجاد شده کپی کرده و جایگزین فایل `.env` کنید:

```bash
cp .env.secure .env
```

**مهم**: رمز عبور را تغییر دهید!
```bash
# ویرایش فایل .env و تغییر رمز عبور به مقدار امن
DB_PASSWORD=YourSecurePassword123!@#
```

### مرحله ۳: تنظیمات API امن

کلیدهای API را به مکان امن منتقل کنید:
- کلید SMSIR را در متغیر محیطی یا فایل پیکربندی امن قرار دهید
- از استفاده از کلیدهای هاردکد شده خودداری کنید

### مرحله ۴: بررسی امنیت سرور

```bash
# بررسی فایل‌های حساس
ls -la .env*

# اطمینان از محافظت فایل‌ها
chmod 600 .env
chmod 644 config/database.php
```

## چک‌لیست امنیتی

- [ ] اجرای اسکریپت `database_user_setup.sql`
- [ ] کپی فایل `.env.secure` به `.env`
- [ ] تغییر رمز عبور پایگاه داده به مقدار امن
- [ ] غیرفعال کردن حالت دیباگ
- [ ] تنظیم `APP_ENV=production`
- [ ] انتقال کلیدهای API به مکان امن
- [ ] بررسی دسترسی فایل‌های حساس
- [ ] تست اتصال پایگاه داده با اعتبارنامه‌های جدید

## نکات امنیتی مهم

1. **هرگز از کاربر root استفاده نکنید**
2. **رمز عبور قوی انتخاب کنید** (حداقل ۱۲ کاراکتر با حروف، اعداد و نمادها)
3. **فایل‌های .env را در کنترل نسخه قرار ندهید**
4. **کلیدهای API را رمزگذاری کنید**
5. **حالت دیباگ را در production غیرفعال کنید**

## تست امنیت

پس از اعمال تغییرات، سیستم را تست کنید:

```bash
# پاک کردن cache
php artisan config:clear
php artisan cache:clear

# تست اتصال پایگاه داده
php artisan tinker
# سپس تایپ کنید: DB::connection()->getPdo();
```

## پشتیبانی

در صورت بروز مشکل، لاگ‌های Laravel را بررسی کنید:
```
storage/logs/laravel.log
```