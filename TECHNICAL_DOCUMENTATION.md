# مستندات فنی و گزارش بهینه‌سازی پروژه پارس لیان

این مستند شامل جزئیات تغییرات معماری، بهینه‌سازی‌های انجام شده و راهنمای توسعه برای ساختار جدید پروژه است.

## ۱. تحلیل معماری جدید

معماری پروژه از مدل **Fat Controller** به سمت **Service Layer Architecture** تغییر یافته است. این تغییر باعث جداسازی منطق تجاری (Business Logic) از لایه ارائه (Presentation Layer) شده است.

### اجزای اصلی:
- **Controllers**: تنها مسئول مدیریت درخواست‌های HTTP، اعتبارسنجی ورودی‌ها (Request Classes) و بازگرداندن پاسخ‌ها هستند.
- **Services**: تمام منطق تجاری سیستم در این لایه متمرکز شده است.
- **Models**: مسئول تعامل با پایگاه داده و تعریف روابط هستند.
- **Enums**: برای مدیریت وضعیت‌ها و مقادیر ثابت استفاده می‌شوند تا از بروز خطاهای انسانی جلوگیری شود.

## ۲. سرویس‌های ایجاد شده

### `OrderService`
این سرویس مدیریت کامل چرخه حیات یک سفارش سرویس را بر عهده دارد.
- **متدها**: `createOrder`, `updateOrder`, `updateStatus`, `processAttachments`.
- **ویژگی‌ها**:
    - استفاده از Database Transactions برای تضمین یکپارچگی داده‌ها.
    - مدیریت خودکار ارسال SMS و ثبت لاگ تغییر وضعیت.
    - اعتبارسنجی هوشمند تغییرات وضعیت (Valid Status Transitions).

### `RepairService`
این سرویس منطق تخصصی مربوط به بخش تعمیرات را مدیریت می‌کند.
- **متدها**: `addItem`, `updateCosts`, `assignTechnician`, `discharge`.
- **ویژگی‌ها**:
    - جداسازی منطق محاسبات هزینه تعمیرات.
    - یکپارچگی با `AccountingManager` برای ثبت خودکار فاکتور در هنگام ترخیص.

## ۳. بهینه‌سازی‌های انجام شده

### الف) کارایی و عملکرد (Performance)
- **رفع مشکل N+1 Queries**: با استفاده از Eager Loading (`with(['...'])`) در کنترلرهای اصلی و داشبورد، تعداد کوئری‌های پایگاه داده به شدت کاهش یافته است.
- **Caching**: سیستم کشینگ برای آمارهای داشبورد پیاده‌سازی شده تا فشار روی پایگاه داده در صفحات پربازدید کاهش یابد.

### ب) کیفیت کد (Code Quality)
- **Dependency Injection**: استفاده از تزریق وابستگی در کنترلرها برای افزایش تست‌پذیری و کاهش وابستگی‌های سخت (Tight Coupling).
- **Single Responsibility**: هر کلاس تنها یک وظیفه مشخص دارد. برای مثال، منطق خروجی CSV به کلاس `ServiceOrderExport` منتقل شد.
- **Don't Repeat Yourself (DRY)**: کدهای تکراری برای ایجاد دستگاه، پردازش فایل‌های ضمیمه و ارسال SMS حذف و در سرویس‌ها تجمیع شدند.

### ج) پایداری (Stability)
- **Atomic Operations**: استفاده از `DB::transaction` تضمین می‌کند که در صورت بروز خطا در هر مرحله از ثبت سفارش یا ترخیص، داده‌ها به حالت قبل بازمی‌گردند.
- **Error Handling**: افزودن بلوک‌های `try-catch` و لاگ‌گذاری دقیق خطاها در لایه‌های حساس.

## ۴. راهنمای مهاجرت و توسعه (Migration Guide)

برای افزودن قابلیت‌های جدید یا تغییر در منطق فعلی، مراحل زیر را دنبال کنید:

1. **تغییر منطق تجاری**: همیشه تغییرات را در کلاس `Service` مربوطه اعمال کنید، نه در `Controller`.
2. **استفاده از سرویس در کنترلر**: سرویس مورد نظر را در سازنده (Constructor) یا متد کنترلر تزریق کنید.
3. **تغییر وضعیت‌ها**: برای افزودن وضعیت جدید، ابتدا آن را در `ServiceOrderStatus` (Enum) تعریف کرده و سپس قوانین انتقال را در `OrderService->isValidStatusTransition` بروزرسانی کنید.
4. **کوئری‌های جدید**: در هنگام دریافت لیست داده‌ها، همیشه روابط مورد نیاز را `eager load` کنید تا کارایی سیستم حفظ شود.

## ۵. معیارهای سنجش موفقیت

- **تعداد کوئری‌ها**: کاهش حداقل ۵۰ درصدی تعداد کوئری‌ها در صفحات لیست و داشبورد.
- **خوانایی کد**: کاهش تعداد خطوط کدهای تکراری در کنترلرها.
- **نگهداری**: امکان تغییر در منطق ارسال SMS یا ثبت لاگ تنها با تغییر در یک متد در `OrderService`.
- **توسعه‌پذیری**: سرعت بالاتر در افزودن ویژگی‌های جدید به دلیل ساختار ماژولار سرویس‌ها.
