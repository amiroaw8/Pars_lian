# مستندات API پروژه پارس لیان (Pars Lian)

این فایل شامل مستندات APIهای موجود در پروژه پارس لیان است. تمامی پاسخ‌ها با فرمت JSON ارسال می‌شوند.

---

## احراز هویت و امنیت

تمامی درخواست‌های API تحت محدودیت‌های زیر هستند:

- **Rate Limiting:**
  - مسیرهای عمومی API: ۶۰ درخواست در دقیقه برای هر IP یا کاربر.
  - مسیرهای احراز هویت: ۵ درخواست در دقیقه برای هر IP.
  - بخش پرداخت و تکمیل سفارش: ۳ درخواست در دقیقه برای هر کاربر.
- **Security Headers:** هدرهای امنیتی استاندارد (XSS Protection, CSP, HSTS و غیره) در تمامی پاسخ‌ها لحاظ می‌شوند.
- **Input Sanitization:** تمامی ورودی‌های رشته‌ای به صورت خودکار پاکسازی (Strip Tags) می‌شوند.

---

## ۱. محصولات (Products)

### دریافت لیست محصولات
لیست محصولات فعال را به صورت صفحه‌بندی شده باز می‌گرداند.

- **آدرس:** `/api/v1/products`
- **متد:** `GET`
- **پارامترهای کوئری:**
  - `category` (string): فیلتر بر اساس اسلاگ دسته‌بندی
  - `search` (string): جستجو در نام محصول
  - `per_page` (int): تعداد در هر صفحه (پیش‌فرض: ۱۰)
- **پاسخ موفق (200 OK):**
```json
{
  "current_page": 1,
  "data": [
    {
      "id": 1,
      "name": "نام محصول",
      "slug": "product-slug",
      "price": "1000.00",
      "category": { "id": 1, "name": "نام دسته‌بندی" }
    }
  ],
  "total": 1
}
```

### دریافت جزئیات یک محصول
اطلاعات کامل یک محصول خاص را باز می‌گرداند.

- **آدرس:** `/api/v1/products/{product_id}`
- **متد:** `GET`
- **پاسخ موفق (200 OK):** اطلاعات محصول به همراه دسته‌بندی.
- **پاسخ خطا (404 Not Found):** در صورتی که محصول وجود نداشته باشد یا غیرفعال باشد.

### ایجاد محصول جدید
- **آدرس:** `/api/v1/products`
- **متد:** `POST`
- **بدنه درخواست (JSON):**
  - `name` (string, required): نام محصول
  - `sku` (string, required): کد کالا (یکتا)
  - `price` (numeric, required): قیمت
  - `category_id` (int, required): شناسه دسته‌بندی
- **پاسخ موفق (201 Created):** اطلاعات محصول ایجاد شده.

### بروزرسانی محصول
- **آدرس:** `/api/v1/products/{product_id}`
- **متد:** `PUT/PATCH`
- **بدنه درخواست (JSON):**
  - `name` (string, optional)
  - `price` (numeric, optional)
  - `stock_quantity` (int, optional)
- **پاسخ موفق (200 OK):** اطلاعات محصول بروزرسانی شده.

---

## ۲. انواع دستگاه‌ها (Device Types)

### دریافت زیرمجموعه‌های یک نوع دستگاه
- **آدرس:** `/api/device-types/children/{name}`
- **متد:** `GET`
- **توضیح:** بر اساس نام (name) دستگاه، زیرمجموعه‌های مستقیم آن را برمی‌گرداند.
- **پاسخ موفق (200 OK):** آرایه‌ای از اشیاء DeviceType.

### دریافت مدل‌ها/واریانت‌های یک مدل
- **آدرس:** `/api/device-types/variants/{modelName}`
- **متد:** `GET`
- **توضیح:** مشابه متد children، برای دریافت واریانت‌های یک مدل خاص استفاده می‌شود.
- **پاسخ موفق (200 OK):** آرایه‌ای از اشیاء DeviceType.

---

## ۳. کاربران (Users)

### دریافت اطلاعات کاربر فعلی
- **آدرس:** `/api/user`
- **متد:** `GET`
- **توضیح:** اطلاعات کاربر لاگین شده را برمی‌گرداند (نیاز به احراز هویت Sanctum).
- **پاسخ موفق (200 OK):** اطلاعات کاربر.
